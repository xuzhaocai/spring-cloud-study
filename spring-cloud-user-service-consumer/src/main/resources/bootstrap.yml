spring:
  cloud:
    config:
      # 配置文件的名字
      name: spring-cloud-user-service-consumer
      # 配置文件后缀，主要是用来区分环境版本
      profile: dev
      # 分支
      label: master
      # spring cloud  config server的地址
      uri: ["http://127.0.0.1:9060"]
  rabbitmq:
    host: 116.85.26.130
    port: 5672
    username: guest
    password: guest
